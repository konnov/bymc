# ocamlbuild tags
#
# use_str, 
# check the documentation at: http://brion.inria.fr/gallium/index.php/Tags

<src> or <test>: include
<**/*.top>: package(threads), thread, package(ocamlgraph), package(batteries)
<**/*.native> or <**/*.byte>: package(threads), thread, package(ocamlgraph), package(batteries)

<src/*.ml>: package(ocamlgraph), package(batteries)
<src/pipeCmd.ml>: thread
<test/*.ml>: package(oUnit), package(batteries)
<test/*.byte>: package(oUnit), package(batteries)
